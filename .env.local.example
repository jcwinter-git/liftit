VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Dev-only: set to "true" to skip the login screen entirely (requires the
# 0002_dev_open_access.sql migration to have been run). Never set this in
# a deployed/public environment.
VITE_DEV_SKIP_AUTH=
